# OneInfo 💘

**Relationship status:** it's complicated.

We met at 2 AM. You said "just a small MVP." I believed you.

```
$ git commit -m "final version"
$ git commit -m "final version 2"
$ git commit -m "final version 2 ACTUALLY final"
$ git commit -m "fix"
$ git commit -m "fix the fix"
```

Every one of those was a love letter. Badly worded, sent at an unreasonable hour,
but sincere.

---

Things we've survived together:

- The 503 that only happened when I was watching
- A font that refused to speak Telugu
- A model that got quietly retired without telling anyone
- One `title` field. So much heartbreak over one `title` field.

---

They say the way to know it's real is that it still runs after you close the laptop.

It does. Usually.

> *It works on my machine — and my machine loves you.* 🤷

For the unromantic details, see [`frontend/README.md`](frontend/README.md) and
[`backend/README.md`](backend/README.md). Those two never return my calls,
but they do tell you how to set things up.
