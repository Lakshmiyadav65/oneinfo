# OneInfo

```
$ git commit -m "final version"
$ git commit -m "final version 2"
$ git commit -m "final version 2 ACTUALLY final"
$ git commit -m "fix"
$ git commit -m "fix the fix"
```

It works on my machine. 🤷

> Documentation is like sex: when it's good, it's very good.
> When it's bad, it's better than nothing.

Real docs live in [`frontend/README.md`](frontend/README.md) and
[`backend/README.md`](backend/README.md), because past me was briefly responsible.
